---
title: "Statistik"
subtitle: "Del 1" 
format: revealjs
engine: knitr
filters: 
  - webr
webr:
  packages: ["gt", "tidyverse"]
---

<style>
/* Targetter din custom webr-extension */
.r-wasm-editor {
  float: left;
  width: 48%;
  height: 80vh;
  box-sizing: border-box;
  padding-right: 10px;
  overflow: auto;
}

.r-wasm-output {
  float: right;
  width: 48%;
  height: 80vh;
  box-sizing: border-box;
  padding-left: 10px;
  overflow: auto;
}

/* Clearfix */
.slide:after {
  content: "";
  display: table;
  clear: both;
}
</style>
{{< include ../_extensions/r-wasm/live/_knitr.qmd >}}

  

## Indhold

* Formålet med statistik
* Målet med statistik
* Stikprøve vs. population
* parametre vs statistik
* Kategoriske data
* Kvantitative data
* Deskriptiv statistik
* placering af data
* Spredning af data
* CFD
* Normalfordelingen
* Den centrale grænseværdisætning
* Hypotesetest



## Formålet med statistik

”Statistics is the science of collecting, summarizing, presenting and interpreting data, and of using them to estimate the magnitude of associations and test hypotheses. It has a central role in medical investigations” Indledende afsnit i lærebogen ”Medical statistics””

:::{.notes}
Det vil sige at vi bruger statistik til at forstå, estimere og udlede 
information, baseret på data.
:::

## Målet med statistik

:::{.incremental}
* Deskriptiv statistik
  - Histogrammer
  - Gennemsnit, median
  - Spredning, percentiler, range
* Inferens
  - Generel information fra en stikprøve
  - Hypotesetest og konfidensintervaller
* Prædiktion
  - Forudsige udfald baseret på data
:::

:::{.notes}
Deskriptiv til at forstå data. 
Inferens - til at drage konklusioner om en hel population ud fra en stikprøve
Det er sådan noget som hypotesetest og konfidensintervaller.
Forudisge fremtidige eller ukendte observation baseret på de data vi har.
:::

## Stikprøve vs population

* Populationen er det komplette sæt af entiteter† vi ønsker at lære noget om
* Stikprøven er den del mængde af det komplette, som vi har data for
* Populationen indeholder stikprøven. Men stikprøven dækker sjældent hele populationen‡

Vi ønsker ofte at kunne sige noget om populationen, baseret på stikprøven. Det kalder vi inferens.

†) Alle børn i Danmark. Alle heste i Danmark. Koncentrationen af Magnesium i drikkevandet - over alt!
‡) Danske registerdata kan være undtagelsen


## Fordelinger

Der er i populationen aldrig en enkelt værdi. Der er altid flere.

Når vitager en stikprøve får vi aldrig en enkelt værdi. Der er altid flere.

Eller - der er en fordeling af værdier. 

Så vi har en fordeling af værdier i populationen - det er den "sande" fordeling.

Og vi har en fordeling af værdier i stikprøven - det er fordelingen i vores _stikprøve_

## Parametre vs statistik

Parametre beskriver fordelingen i _populationen_.

(delvist) Teoretiske tal - de eksisterer, men vi kan ikke måle dem.

Den sande værdi af den gennemsnitlige højde af mænd i Danmark findes. Men vi ved ikke hvad
den er, for vi kan ikke måle højden af alle mænd. Det sande gennemsnit kalder vi $\mu$.

Der er en sand variation (vi kommer til det) af højden af alle mænd i Danmark. Den findes,
og vi kalder den $\sigma^2$. Men vi ved ikke hvad den er, og vi kan ikke finde ud af hvad den er.

Statistikken derimod er det vi ved hvad er. Det er gennemsnitshøjden af alle mænd i vore stikprøve.
Den kalder vi $\hbar{X}$. Variansen kan vi også beregne. Den kalder vi $s^2$

Bemærk forskellen i notationen.

## Fra statistik til parametre

Vi kan bruge $\hbar{X}$ og $s^2$ til at estimere de ukendte parametre: $\mu$ og $\sigma^2$

Hvis man vil kunne huske hvad non-parametriske tests er - så skal man kunne huske det her.

## Typer af data - kategoriske

Data falder i _en_ kategori. Eller i _en_ anden. _Ikke_ i begge.
Vi kalder dem ofte kvalitative.
:::{.incremental}
* Nominale data
  - Der er ingen rækkefølge - ingen er "bedre" end andre
    - Yndlingsfarve
    - mand/kvinde
    - Ja/nej
* Ordinale data
  - Stadig kategoriske
  - Der er en naturlig rækkefølge
    - Højde: Lav, medium, høj
    - Terningekast
    - Socialklasse
:::

## Typer af data - kvantitative

Numeriske data - målinger eller optællinger

:::{.incremental}
* Diskrete
  - Kun specifikke værdier er mulige
    - Antal børn
    - Antal senge på et hospital
* Kontinuærte
  - Målbare enheder, med mulige værdier der ligger i et interval
    - BMI
    - Blodtryk
    - Højde

:::

:::{.notes}
Kvantitative data er typisk noget vi måler. Og i princippet kan vi måle
med arbitrær præcision.
Kvalitative data er data der kun kan være i en kasse. Enten er man 
gravid. Eller også er man ikke. Man er ikke en lille smule gravid. 
Der er to typer. Nominale hvor der ikke er nogen orden. Kvinder er ikke 
bedre end mænd.
Ordinale data - og der er en orden. Det er (ofte) bedre at slå en sekser
end en treer når man kaster med terninger. 
:::

## Her kunne vi have noget om visning af data

Men det tror jeg hellere jeg vil have senere


## Deskriptiv statistik til beskrivelse af data

:::: {.columns}

::: {.column width="60%"}
Hvordan kan vi beskrive datasættet til højre?

:::{.incremental}
* Minimum
* Maximum
* Gennemsnit
* Spredning
* Normalområde
* Histogram
:::

:::

::: {.column width="40%"}
```{r echo = FALSE}
library(gt)
library(tidyverse)
haem_data <- c(10.2, 13.3, 10.6, 12.1,
9.3,12.0,13.4,11.9,11.2,14.6, 13.7,
12.9,10.5,12.9,13.5,12.9,12.1,11.4,15.1,11.1, 10.4,12.1,13.7,11.4 ,14.6,11.1,10.9,12.5,10.7,13.5, 14.9,9.4,11.8,12.7,11.2,8.8,11.3,13.0,12.9,10.9, 11.5,13.2,14.1,10.6,11.7,10.2,14.7, 11.6,13.4,13.1, 12.0,10.8,10.3,11.4,10.9,11.6,10.8,13.1,12.3,11.8, 11.0,11.7,13.6,11.9,10.4,12.5,13.3,9.7,11.0,12.2) 
haem_data |> 
  matrix(ncol = 7, byrow =FALSE) |>  data.frame() |> 
  gt() |> 
  tab_options(column_labels.hidden = TRUE) |> 
  tab_footnote("Haemoglobin levels in g/100 ml for 70 women")
```
:::

::::



## Deskriptiv statistik til beskrivelse af data {visibility="true"}

Placering. Hvor ca. ligger data?

* Middelværdi:  $\bar{x} = \frac{1}{n}\big(\sum_{i=1}^{n}x_i\big) = \frac{x_1 + x_2 + ... + x_n}{n}$
  - Følsom(ish) overfor outliers
* Median: Den midterste værdi - når alle værdier er stillet op fra mindste til største
  - Halvdelen af observationerne ligger over medianen. Halvdelen under.
  - Ufølsom(ish) overfor outliers

Det beregner vi "let" i R:

```{webr}
#| context: setup
#| autorun: true
haem_data <- c(10.2, 13.3, 10.6, 12.1,
9.3,12.0,13.4,11.9,11.2,14.6, 13.7,
12.9,10.5,12.9,13.5,12.9,12.1,11.4,15.1,11.1, 10.4,12.1,13.7,11.4 ,14.6,11.1,10.9,12.5,10.7,13.5, 14.9,9.4,11.8,12.7,11.2,8.8,11.3,13.0,12.9,10.9, 11.5,13.2,14.1,10.6,11.7,10.2,14.7, 11.6,13.4,13.1, 12.0,10.8,10.3,11.4,10.9,11.6,10.8,13.1,12.3,11.8, 11.0,11.7,13.6,11.9,10.4,12.5,13.3,9.7,11.0,12.2) 
```

```{webr}
mean(haem_data)
median(haem_data)
```

::::notes
Vis gerne data i kodeblokken. 
Hvad hvis der er et lige antal observationer? Så tager vi gennemsnittet af de to midterste
::::

## Modus

Hvad er gennemsnittet af hårfarve?

Kategoriske værdier kan vi ikke beregne middelværdier for. Eller medianer

Istedet: Modus (engelsk "mode"). Ofte "typetal"

Den hyppigst forekommende observation (der kan være mere end en, men så er de lige store)

Ikke følsom overfor outliers. Særligt nyttig ved kvalitative data.

Ikke en indbygget funktion i R!

## Ingen modus i R? 
No problem

Modus for `Species` i datasættet `iris`:
```{webr}
head(penguins)
table(penguins$species) |> sort(decreasing = TRUE) |>
```

## Deskriptiv statistik til beskrivelse af data - variation

Der er variation i vores data. Gennemsnittet af højden for alle mænd i vores stikprøve er
muligvis 182 cm. Men nogen er højere, og andre lavere.

Variationen - hvordan er data spredt?

Hvordan kan vi beskrive det?


Hvor meget afviger de enkelte observation fra gennemsnittet

Intuitivt kan vi se at ikke alle værdierne ligger lige tæt på gennemsnittet. Så. Vi kan beregne hvor 
langt hver værdi ligger fra gennemsnittet. Hvis vi bare lægger de tal sammen, så bliver størrelsen
afhængig af hvor mange data punkter vi har. s Vi kan have 10000 punkter der alle sammen ligger utroligt tæt
på gennemsnittet. Eller vi kan have 10 punkter der alle ligger meget langt fra gennemsnittet. Vi har brug for 
et gennemsnit. Problemet er, at gennemsnittet af forskellen for de enkelte punkter fra gennemsnittet, vil give 0.
I stedet tager vi kvadratet på alle forskellene - og beregner gennemsnittet af dem. Det betyder også at punkter der
ligger langt fra gennemsnittet "fylder" mere end punkter der ligger tæt på gennemsnittet. 

Det kalder vi for variansen.

Der er et problem med den. Enheden. Enheden af vores hæmoglobin målinger bliver

$g^2/ml^2$
Den giver ingen mening. I stedet ser vi på standardafvigelsen, hvor vi tager kvadratroden på variansen.

## formlerne

* Varians: $\sigma^2 = \frac{1}{N}\sum_{i = 1}^{N}(x_i - \mu)^2$
* Spredning: sd = $\sigma = \sqrt{\frac{1}{N}\sum_{i = 1}^{N}(x_i - \mu)^2}$


## Deskriptiv statistik til beskrivelse af data

Hvordan fordeler data sig?

Histogrammet er en frekvenstabel hvor vi opgiver frekvenser (antallet) af 
værdier indenfor (ens) intervaller:

```{r}
tibble(x = haem_data) |> 
  ggplot(aes(x)) +
  geom_histogram()
```

Meget afhængig af hvilke intervaller man vælger!

```{r}
tibble(x = haem_data) |> 
  ggplot(aes(x)) +
  geom_histogram(breaks= 8:16)

```



## cfd

Ud fra histogrammet får vi en ok ide om hvordan data fordeler sig.

Vi kan også vælge en mere finkornet visualisering, Den Empiriske Kumulative 
Tæthed af data. Vi har 70 observationer. Hver af dem svarer derfor til
`r 100/70`%

```{r}
tibble(x = haem_data) |> 
  ggplot(aes(x)) + 
  stat_ecdf(geom = "step")
```

Det her er vigtigt at forstå. 

Hvor ligger kvartilerne?

Hvor ligger 2½ procent percentilen?

Ud fra det kan vi aflæse ved hvilken værdi 25% af observationerne ligger over. Og 75% ligger under. 

Vi kan aflæse en hvilken som helst percentil

## Hvad med den der normalfordeling?

Vi har set hvordan vores data fordeler sig. De følger sådan nogenlunde
normalfordelingen. Men hvad er det?

Hvordan ser normalfordelingen ud?

$$f(x) = \frac{1}{\sqrt{2\pi\sigma^2}}e^{-\frac{(x-\mu)^2}{2\sigma^2}}$$

Den har nogle rare matematiske egenskaber

Bemærk: Afhænger af to parametre. Middelværdien ($\mu$), og 
standardafvigelsen $\sigma$ (eller er det variansen?)
Kender vi dem, kan vi beskrive alt ved den fordeling.
 
## grafisk

```{r echo = FALSE}
library(grid)
library(gtable)
library(tidyverse)


markers <- tibble(x = seq(from = -3, to = 3, by = 0.5)) |> 
   mutate(y=dnorm(x))

labels <- tribble(~x, ~y, ~label,
-0.25 , dnorm(-0.25)/2, "19.1%",
0.25 , dnorm(0.25)/2, "19.1%",
-0.75, dnorm(0.75)/2, "15.0%",
0.75, dnorm(0.75)/2, "15.0%",
-1.25, dnorm(1.25)/3, "9.2%",
1.25, dnorm(1.25)/3, "9.2%",
-1.75, dnorm(2.25), "4.4%",
1.75, dnorm(2.25), "4.4%",
-2.25, dnorm(2.25)/2, "1.7%",
2.25, dnorm(2.25)/2, "1.7%",
2.75, dnorm(2.75)*2, "~0.5%",
3.25, dnorm(3.25)*4, "~0.1%",
-2.75, dnorm(2.75)*2, "~0.5%",
-3.25, dnorm(3.25)*4, "~0.1%")


# Samme lineheight begge steder
lh <- 1.00

breaks_1 <- seq(-3, 3, by = 1)
sigma_labels <- c("-3σ","-2σ","-1σ","0","+1σ","+2σ","+3σ")
z_labels     <- as.character(breaks_1)
cum_labels   <- c("0.1%","2.3%","15.9%","50%","84.1%","97.7%","99.9%")
tick_labels  <- paste(sigma_labels, z_labels, cum_labels, sep = "\n")


p <- ggplot(data = data.frame(x = c(-4, 4)), aes(x)) +
  stat_function(fun = dnorm, n = 101, args = list(mean = 0, sd = 1)) + ylab("") +
  scale_y_continuous(breaks = NULL) +
  geom_segment(data = markers, aes(x=x, y =0, xend = x, yend = y)) +
  geom_text(data = labels, mapping= aes(x,y, label = label)) +
  scale_x_continuous(breaks = breaks_1, labels = tick_labels, name = NULL) +
  annotate("segment", x = -1, y = dnorm(1), xend = 1, yend = dnorm(1), color = "red") +
  annotate("text", x = 0, y = dnorm(0.97), label = "68%", color = "RED") +

  annotate("segment", x = -1.96, y = dnorm(1.96), xend = 1.96, yend = dnorm(1.96), color = "blue") +
  annotate("text", x = 0, y = dnorm(1.9), label = "95%", color = "BLUE") +
  theme(line = element_blank(), rect = element_blank())


# --- Tilføj venstre kolonne med 3-linjers label, med vertikal nudge ---
gt <- ggplotGrob(p)
axis_b_id <- which(gt$layout$name == "axis-b")
axis_b    <- gt$layout[axis_b_id, ]

# Justér denne for at løfte/sænke venstreteksten i forhold til tick-linjerne
y_nudge_lines <- 0.9  # positiv = lidt op, negativ = lidt ned

left_lab <- textGrob(
  "Standard Deviation\nZ-Score\nCumulative Percent",
  x = unit(1, "npc"),
  just = c("right","top"),
  gp = gpar(fontsize = 10, lineheight = lh)
)
# Flyt grob'en en smule op i samme celle som x-aksen
left_lab$vp <- viewport(y = unit(1, "npc") + unit(y_nudge_lines, "lines"),
                        just = c("center","top"))

# Kolonnebredde baseret på tekstens faktiske bredde + lidt luft
left_w <- grobWidth(left_lab) + unit(0.6, "lines")

gt <- gtable_add_cols(gt, widths = left_w, pos = axis_b$l - 1)
left_col <- axis_b$l

gt <- gtable_add_grob(
  gt, left_lab,
  t = axis_b$t, b = axis_b$b,
  l = left_col, r = left_col,
  clip = "off"
)

grid.newpage()
 grid.draw(gt)


```

## Hvordan fandt vi de værdier?

```{r echo = FALSE}
library(tidyverse)


markers <- tibble(x = c(-1,1)) |> 
   mutate(y=dnorm(x))



p <- ggplot(data = data.frame(x = c(-4, 4)), aes(x)) +
  stat_function(fun = dnorm, n = 101, args = list(mean = 0, sd = 1)) + ylab("") +
    theme(line = element_blank(), rect = element_blank()) +
  geom_segment(data = markers, aes(x=x, y =0, xend = x, yend = y)) +
  
  annotate("segment", x = -2, y = 0.25, xend = -1, yend=dnorm(-1), arrow = arrow()) +
  annotate("text", x = -2, y = 0.28, label = "dnorm(-1) = 0.242") +
  annotate("segment", x = 2, y = 0.25, xend = 1, yend=dnorm(1), arrow = arrow()) +
  annotate("text", x = 2, y = 0.28, label = "dnorm(1) = 0.242") +
   stat_function(fun = dnorm, args = list(mean = 0, sd = 1),
                geom = "area", xlim = c(-4, -1), alpha = 0.4) +
  annotate("text", x = -1.4, y = 0.07, label = "pnorm(-1) \n = 15.9%") +
annotate("segment", x = -3, y = 0.15, xend = -1, yend=0, arrow = arrow()) +
  annotate("text", x = -3, y = 0.17, label = "qnorm(0.159) = ~1") +
  stat_function(fun = dnorm, args = list(mean = 0, sd = 1),
                geom = "area", xlim = c(-1, 1), alpha = 0.4, fill = "red") +
  annotate("text", x = 0, y = 0.17, label = "pnorm(1) - pnorm(-1) = 68.3%") 
p +
  annotate("text", x = -2, y = 0.4, label = "15.9% af data er mindre end -1 sigma \n 68.3% af data ligger mellem -1 og 1 sigma")


```

## Øvelse

* Hvor mange % af data er mindre end 1.5 sigma?
* Hvor mange % af data ligger mellem 1 og 2 sigma?
* Hvis jeg vil have de 10% største værdier af data - hvilken sigma er de så større end? (hint husk at qnorm giver os værdierne fra -uendelig)

```{webr}

```

## Løsning

```{webr}
print(pnorm(1.5))
print(pnorm(2) - pnorm(1))
print(qnorm(0.9))
```


## Mange normalfordelinger


Den normalfordeling vi har set er standardiserede. Hvor $\mu = 0$
og $\sigma = 1$. Men der er mange andre! Prøv at ændre middelværdien og standardafvigelsen.
<style>
/* Hele webR-blokken: fyld slide-højden */
.qwebr-interactive-area[data-id="mange_normalfordelinger"]{
  width: 100%;
  height: 80vh;
  box-sizing: border-box;
}

/* Toolbar i toppen med fast højde */
.qwebr-interactive-area[data-id="mange_normalfordelinger"] .qwebr-editor-toolbar{
  --toolbar-h: 44px;          /* justér hvis din toolbar er højere/lavere */
  height: var(--toolbar-h);
  box-sizing: border-box;
  float: none;
}

/* Venstre/højre kolonne: fyld resten af højden, scrol indeni (ikke hele siden) */
.qwebr-interactive-area[data-id="mange_normalfordelinger"] .qwebr-console-area,
.qwebr-interactive-area[data-id="mange_normalfordelinger"] .qwebr-output-graph-area{
  height: calc(80vh - var(--toolbar-h));
  overflow: auto;
  box-sizing: border-box;
}

/* Bevar dine floats og bredder */
.qwebr-interactive-area[data-id="mange_normalfordelinger"] .qwebr-console-area{
  float: left;
  width: 48%;
}
.qwebr-interactive-area[data-id="mange_normalfordelinger"] .qwebr-output-graph-area{
  float: right;
  width: 48%;
}

/* Få canvas til at fylde kolonnen pænt */
.qwebr-interactive-area[data-id="mange_normalfordelinger"] .qwebr-output-graph-area figure.qwebr-canvas-image{ 
  margin: 0;
  height: 100%;
}
.qwebr-interactive-area[data-id="mange_normalfordelinger"] .qwebr-output-graph-area canvas{
  display: block;
  width: 100% !important;
  height: auto !important;   /* bevar aspect ratio */
  max-height: 100%;
}

/* (Valgfrit) skjul tekst-output, så det ikke skubber højden */
.qwebr-interactive-area[data-id="mange_normalfordelinger"] .qwebr-output-code-area{
  display: none;
}



</style>

```{webr}
#| label: mange_normalfordelinger
#| echo: false
#| autorun: true
#| warning: false
#| message: false
#| fig-width: 6
#| fig-height: 4
#| out-width: 100%

mu <- 0; sigma <- 1

xmin <- -4; xmax <-  4  # justér hvis grafen bliver væk

library(ggplot2)

ggplot() +
  stat_function(fun = dnorm, args = list(mean = mu, sd = sigma), n = 201, xlim = c(xmin, xmax)) 
```


## Med andre ord...

Hvis vi har noget data der er normalfordelt
Og vi kender middelværdi og standardafvigelse
Så kan vi beregne inden for hvilket interval 95% af observationerne ligger.

```{webr}
qnorm(0.975) # for hvilken x er 97.5% af observationerne mindre?
qnorm(0.025) # for hvilken x er 2.5% af observationerne mindre
pnorm(1.96) # hvor mange % af observationerne er mindre end 1.96?
pnorm(-1.96) # hvor mange % af observationerne er mindre end -1.96
```

## Og hvis data ikke er standardiserede:

Hvor mange procent af observationerne er mindre end 2, hvis middelværdien er 4 og standardafvigelsen er 2:

```{webr}
pnorm(2, mean = 4, sd = 2 )
```


## Hvordan standardiseres egne data?

* Vi har lov at trække en værdi fra vores observationer. 
* Vi har også lov at gange eller dividere vores observationer med en værdi.
* Så længe vi gør det samme med alle observationerne!

```{webr}
demo <- c(1,47,5,8)
middel <- mean(demo)
stdafv <- sd(demo)
norm_demo <- (demo - middel)/stdafv
```

## Og hvad skal vi så bruge det til?

::::notes
Mit kriterium for om matematik er avanceret: Hvis jeg synes det er svært at forstå, men jeg dog kan forstå det. Så er det 
ikke meget avanceret matematik. For så god er jeg heller ikke til matematik. 
CLT er ikke avanceret. Men jeg synes stadig det er svært.
::::

Et helt centralt begreb i statistik er grænseværdisætningen. Eller på engelsk Central Limit Theorem (CLT)

Tager vi mange stikprøver i en population, vil middelværdierne af stikprøverne være normalfordelte. Også selvom værdierne i 
populationen ikke er normalfordelte.

## demo1

Det her er i hvertfald ikke normal fordelt
```{r}
library(ggplot2)
x <- runif(10000)

# Læg data i en data frame til ggplot
df <- data.frame(x = x)

# Plot som histogram
ggplot(df, aes(x = x)) +
  geom_histogram(breaks = seq(0,1,0.1), color = "black", fill = "skyblue") +
  labs(
    title = "Histogram af 1000 uniformt fordelte værdier",
    x = "Værdi",
    y = "Frekvens"
  ) +
  theme_minimal() 
```


## demo2

<style>
/* Hele webR-blokken: fyld slide-højden */
.qwebr-interactive-area[data-id="cltdemo"]{
  width: 100%;
  height: 80vh;
  box-sizing: border-box;
}

/* Toolbar i toppen med fast højde */
.qwebr-interactive-area[data-id="cltdemo"] .qwebr-editor-toolbar{
  --toolbar-h: 44px;          /* justér hvis din toolbar er højere/lavere */
  height: var(--toolbar-h);
  box-sizing: border-box;
  float: none;
}

/* Venstre/højre kolonne: fyld resten af højden, scrol indeni (ikke hele siden) */
.qwebr-interactive-area[data-id="cltdemo"] .qwebr-console-area,
.qwebr-interactive-area[data-id="cltdemo"] .qwebr-output-graph-area{
  height: calc(80vh - var(--toolbar-h));
  overflow: auto;
  box-sizing: border-box;
}

/* Bevar dine floats og bredder */
.qwebr-interactive-area[data-id="cltdemo"] .qwebr-console-area{
  float: left;
  width: 48%;
}
.qwebr-interactive-area[data-id="cltdemo"] .qwebr-output-graph-area{
  float: right;
  width: 48%;
}

/* Få canvas til at fylde kolonnen pænt */
.qwebr-interactive-area[data-id="cltdemo"] .qwebr-output-graph-area figure.qwebr-canvas-image{ 
  margin: 0;
  height: 100%;
}
.qwebr-interactive-area[data-id="cltdemo"] .qwebr-output-graph-area canvas{
  display: block;
  width: 100% !important;
  height: auto !important;   /* bevar aspect ratio */
  max-height: 100%;
}

/* (Valgfrit) skjul tekst-output, så det ikke skubber højden */
.qwebr-interactive-area[data-id="cltdemo"] .qwebr-output-code-area{
  display: none;
}
</style>

```{webr}
#| label: cltdemo
#| echo: false
#| autorun: true
#| warning: false
#| message: false
#| fig-width: 6
#| fig-height: 4
#| out-width: 100%

x <- runif(10000)
middelværdier <- numeric() # slet denne linie efter første gang
middelværdi <- mean(sample(x, 10))
middelværdier <- c(middelværdier, middelværdi)
data.frame(x = middelværdier) |> 
  ggplot(aes(x = x)) +
  geom_histogram(aes(y = ..density..),breaks = seq(0,1,0.05), color = "black", fill = "skyblue") +
  stat_function(fun = dnorm, n = 101, args=list(mean= mean(middelværdier), sd = sd(middelværdier)))
```

## Men det er jo _en_ stikprøve!

Så vi bruger xbar i stedet for mu. og s i stedet for sigma. Og hvordan 
passer det så?

Og det er forskelligt fra populationen. 
Og vi tager normalt heller ikke mange stikprøver. Vi har en.

Standardafvigelsen viser variationen i vores stikprøve.

Men vi er egentlig interesseret i hvordan variationen i mange middelværdier 
vil være.

Den beregner vi ved:

$$SEM = \frac{s}{\sqrt{n}}$$

hvor n er antallet af observationer i vores stikprøve.
og s er stikprøvens standardafvigelse


Så det med at beregne en standardafvigelse på mange stikprøver, når vi kun
har en stikprøve. Hvordan håndterer vi det?

For standardafvigelsen fortæller os hvordan observationerne i vores stikprøve
afviger fra middelværdien. Ikke hvordan gennemsnittet ville ændre sig
hvis vi tog en ny stikprøve.

Til det formål bruger vi SEM¨

“SD viser variationen i data – SEM viser usikkerheden på gennemsnittet.”

eller endnu kortere:

“SD = spredning af observationer, SEM = spredning af middelværdier.”


vi skal nok også lige have præciseret hvad det faktisk er der er på
x-aksen. Observationernes afstand fra middelværdien, målt i standardafvigelser.



## Øvelse

Dette er værdier fra R. Datasæt B er den målte hastighed af biler på
en landevej. Vi antager at værdierne i dataæsttet B er normalfordelte.
Beregn et interval som indeholder 95% af B's værdier

På en eller anden måde skal jeg have kontstrueret et datasæt der giver:

c(mean(A), mean(B), mean(Diff)) = 68.59, 81.18, -12.59
c(sd(A), sd(B), sd(Diff))
13.81 15.39 14.16

Da vi kan antage normalfordeling (det kan vi ikke, men opgaven siger vi
kan, så det kan vi) er 95% af værdierne indeholdt i intervallet


```{r}
81.18 + c(-1,1)*qnorm(0.975)*15.39
```

Så - 95% af hastighederne vi har målt på landevejen ligger mellem disse
to værdier. Og! vi kan antage at den fordeling også gælder i baggrundspopulationen
(der altså er alle biler, også dem vi ikke målte på)

## En gang til.

Fordelingen af tilfældigt udtagne stikprøvers gennemsnit er altid 
normalfordelt. 

Tilbage til haem tallene. Det er en tilfældig stikprøve. Det vil sige at 
den estimerer gennemsnittet for populationen. Men ikke præcist. Hvis vi
tager en ny stikprøve, får vi et nyt gennemsnit. 

Men. De forskellige gennemsnit er normalfordelte omkring gennemsnittet af
populationen.

OGSÅ SELVOM POPULATIONEN IKKE SELV ER NORMALFORDELT!


SE, SEM og forskellen mellem dem og sd.


## Og så kan vi gå i gang med hypotesetests!

